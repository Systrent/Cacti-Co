import { Background } from '../../components/shared/Background';

const Weather = () => {
    return (
        <div>
            <Background>Hola</Background>
        </div>
    );
};

export default Weather;
